# Required — Claude API key (used by ideaValidator and analyze functions)
ANTHROPIC_KEY=your_anthropic_api_key_here

# Optional — set to "true" to log each idea validation to Firestore (idea_validations collection)
ENABLE_LOGGING=true

# Required — Resend email API key
RESEND_API_KEY=your_resend_api_key_here
